import React from 'react'

const capturePhoto = () => {
  return (
    <div>capturePhoto</div>
  )
}

export default capturePhoto
// draw camera photo
// load frame image
// draw frame on top of photo